name: helm_ci_conftest

on:

  workflow_call:
    inputs:
      repository: 
        type: string
      path:
        type: string
    secrets:
      pat:
        required: true

#条件  
jobs:
  conftest_helm:
    name: helm-ci-test-linux 
    runs-on: ubuntu-latest
    steps:
      - name: checkout helm_ci_test
        uses: actions/checkout@v3
      - name: rego repository
        uses: actions/checkout@v3
        with:
          repository: ${{ inputs.repository }}
          path: ${{ inputs.path }}
          token: ${{ secrets.pat}} 

      - name: "ls ci-test after checkout"
        run: ls -la
      - name: conf install at conftest_helm
        run: |
          LATEST_VERSION=$(wget -O - "https://api.github.com/repos/open-policy-agent/conftest/releases/latest" | grep '"tag_name":' | sed -E 's/.*"([^"]+)".*/\1/' | cut -c 2-)
          wget "https://github.com/open-policy-agent/conftest/releases/download/v${LATEST_VERSION}/conftest_${LATEST_VERSION}_Linux_x86_64.tar.gz"
          tar xzf conftest_${LATEST_VERSION}_Linux_x86_64.tar.gz
          sudo mv conftest /usr/local/bin/
          conftest --version
          ls -la
#      - name: pwd;ls
#        run: pwd;ls -la
#      - name: helm template
#        run: |
#          mkdir -p ./manifests
#          helm template . > ./manifests/test01.yaml
#      - name: ls manifests_folder
#        run: |
#          ls -la ./manifests/
#          cat ./manifests/test01.yaml
#      - name: conftest 
#        run:  conftest test --policy ./policy ./manifests/test01.yaml
#